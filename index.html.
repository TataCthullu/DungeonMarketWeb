<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Dungeon Market — Khazâd Trading Bot</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            margin: 0;
            font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
            background: #050608;
            color: #f5f5f5;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }
        .container {
            max-width: 800px;
            padding: 24px;
            border-radius: 16px;
            background: radial-gradient(circle at top, #272044 0, #050608 70%);
            box-shadow: 0 0 30px rgba(0,0,0,0.7);
            border: 1px solid rgba(255,255,255,0.08);
        }
        h1 {
            margin-top: 0;
            margin-bottom: 8px;
            font-size: 2rem;
        }
        h2 {
            margin-top: 0;
            font-size: 1.1rem;
            color: #c0a86b;
        }
        p {
            line-height: 1.5;
            margin-bottom: 12px;
        }
        .buttons {
            margin-top: 20px;
            display: flex;
            flex-wrap: wrap;
            gap: 12px;
        }
        a.btn {
            text-decoration: none;
            padding: 10px 18px;
            border-radius: 999px;
            border: 1px solid #c0a86b;
            color: #050608;
            background: #c0a86b;
            font-weight: 600;
            font-size: 0.95rem;
        }
        a.btn.secondary {
            background: transparent;
            color: #c0a86b;
        }
        small {
            display: block;
            margin-top: 16px;
            font-size: 0.8rem;
            color: #a0a0a0;
        }
    </style>
</head>
<body>
    <main class="container">
        <h1>Dungeon Market</h1>
        <h2>Khazâd — Trading Bot para BTC/USDT</h2>

        <p>
            Dungeon Market es un bot de trading escrito en Python con interfaz gráfica
            y estética de dungeon. No es un servicio financiero ni una promesa de rentabilidad:
            es una herramienta para que cada usuario gestione su propia operativa y riesgo.
        </p>

        <p>
            El bot trabaja sobre BTC/USDT y permite configurar parámetros como % de compra/venta,
            inversión por operación, rebalances, compras/vemtas fantasma y más, todo visible desde
            una UI con animaciones.
        </p>

        <div class="buttons">
            <!-- 🔗 acá después ponemos el link real (Itch.io, etc.) -->
            <a class="btn" href="#" target="_blank" rel="noopener">
                Descargar Dungeon Market
            </a>

            <!-- opcional: si después publicás el código en algún lado -->
            <a class="btn secondary" href="#" target="_blank" rel="noopener">
                Ver documentación / detalles
            </a>
        </div>

        <small>
            Uso bajo responsabilidad del usuario. Antes de operar con dinero real, probá siempre en modo
            de prueba / con poco capital.
        </small>
    </main>
</body>
</html>
