<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Dungeon Market — Trading Enviroment</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        /* Fondo general tipo dungeon */
        body {
            margin: 0;
            font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;

            /* Fondo tipo canvas Dungeon Market */
            background-image: url('img/orc_4.png');
            background-repeat: repeat;
            background-size: 128px 128px;  /* probalo en 64 o 256 si querés cambiar */
            
            color: #f5f5f5;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }


        /* “Altar” central, más piedra / grim */
        .container {
            max-width: 860px;
            padding: 32px 32px 26px;
            border-radius: 14px;
            background:
                linear-gradient(145deg, #14101f 0%, #06040a 55%, #17121f 100%);
            box-shadow:
                0 0 35px rgba(0,0,0,0.9),
                0 0 90px rgba(0,0,0,0.9),
                0 0 0 1px rgba(10, 8, 20, 0.9) inset,
                0 -1px 0 rgba(220, 200, 140, 0.10),
                0 1px 0 rgba(0, 0, 0, 0.9);
            border: 1px solid rgba(192,168,107,0.18);
            text-align: center;
        }

        /* Título tipo inscripción rúnica dorada */
        h1 {
            margin: 0 0 6px;
            font-size: 2.6rem;
            color: #dfaa26;
            text-align: center;
            letter-spacing: 0.18em;
            text-transform: uppercase;
            text-shadow:
                0 0 10px rgba(192,168,107,0.55),
                0 0 25px rgba(192,168,107,0.35);
        }

        /* Subtítulo como línea de lore */
        h2 {
            margin: 0;
            font-size: 1.1rem;
            color: #a08954;
            text-align: center;
            letter-spacing: 0.16em;
            text-transform: uppercase;
        }

        /* Línea divisoria tipo runa / grabado */
        .divider {
            width: 140px;
            height: 1px;
            margin: 16px auto 22px;
            background: linear-gradient(
                to right,
                transparent,
                rgba(192,168,107,0.8),
                transparent
            );
            box-shadow: 0 0 12px rgba(192,168,107,0.55);
        }

        p {
            line-height: 1.55;
            margin: 0 0 12px;
            color: #d7d0e0;
            font-size: 0.98rem;
        }

        .buttons {
            margin-top: 22px;
            display: flex;
            flex-wrap: wrap;
            gap: 12px;
            justify-content: center;
        }

        /* Botones como los “paneles” dorados del bot */
        a.btn {
            text-decoration: none;
            padding: 10px 24px;
            border-radius: 8px;
            border: 1px solid #c0a86b;
            background: linear-gradient(to bottom, #c0a86b, #8d7b4f);
            color: #050608;
            font-weight: bold;
            font-size: 0.98rem;
            text-transform: uppercase;
            letter-spacing: 0.08em;
            box-shadow:
                0 0 10px rgba(192,168,107,0.45),
                0 2px 0 rgba(30,20,10,0.8);
            transition: transform 0.15s ease-out, box-shadow 0.15s ease-out, filter 0.15s ease-out;
        }

        a.btn:hover {
            transform: translateY(-1px);
            box-shadow:
                0 0 16px rgba(192,168,107,0.8),
                0 3px 0 rgba(30,20,10,0.9);
            filter: brightness(1.05);
        }

        a.btn.secondary {
            background: #121017;
            color: #c0a86b;
            border: 1px solid #8d7a4a;
            box-shadow:
                0 0 8px rgba(0,0,0,0.8),
                0 2px 0 rgba(0,0,0,0.9);
        }

        a.btn.secondary:hover {
            box-shadow:
                0 0 14px rgba(192,168,107,0.5),
                0 3px 0 rgba(0,0,0,0.95);
        }

        small {
            display: block;
            margin-top: 18px;
            font-size: 0.78rem;
            color: #90889c;
        }
    </style>
</head>
<body>
    <main class="container">
        <h1>Dungeon Market</h1>
        <h2>Trading Enviroment</h2>

        <div class="divider"></div>

        <p>
            Dungeon Market es un entorno de trading basado en la confianza que inspira su transparencia de datos y funcionalidad sin marketing engañoso, con usos útiles de estética de fantasía medieval. No es un servicio financiero ni una promesa de rentabilidad:
            es una herramienta para que cada usuario gestione su propia operativa y riesgo.
        </p>

        <p>
            Las experiencias en las herramientas de trading de fantasía pueden pulir la operativa real.
        </p>

        <div class="buttons">
            <!-- 🔗 acá después ponemos el link real (Itch.io, etc.) -->
            <a class="btn" href="#" target="_blank" rel="noopener">
                Descargar Dungeon Market
            </a>

            <!-- opcional: si después publicás el código en algún lado -->
            <a class="btn secondary" href="#" target="_blank" rel="noopener">
                Ver documentación / detalles
            </a>
        </div>

        <small>
            Uso bajo responsabilidad del usuario. Todos los derechos reservados.
        </small>
    </main>
</body>
</html>
